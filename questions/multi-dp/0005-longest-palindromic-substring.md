---
id: "0005"
slug: "longest-palindromic-substring"
lcId: 5
title: "最长回文子串"
difficulty: "Medium"
category: "multi-dp"
tags:
  - "字符串"
  - "动态规划"
url: "https://leetcode.cn/problems/longest-palindromic-substring/"
planOrder: 93
hasViz: true
hasDiagram: false
feynman:
  essence: "给你一个字符串 `s`，找出其中最长的回文子串。回文就是正着读和反着读一样的字符串，比如 `\"aba\"`、`\"abba\"`。"
  analogy: ""
  key_points: []
first_principle:
  problem: ""
  axioms: []
  rebuild: ""
---

# 5. 最长回文子串

> 难度：**Medium** | 分类：多维动态规划 | 标签：字符串, 动态规划

> 🔗 力扣链接：[https://leetcode.cn/problems/longest-palindromic-substring/](https://leetcode.cn/problems/longest-palindromic-substring/)

## 📝 题目描述

给你一个字符串 `s`，找出其中最长的回文子串。回文就是正着读和反着读一样的字符串，比如 `"aba"`、`"abba"`。

- 题号：5
- 题目名：最长回文子串
- 难度：中等
- 题型：动态规划
- 为什么选这题：它是动态规划和中心扩展两种思路的经典交汇点，训练"从内向外在字符串上扩展"的思维。

## 🧠 思考路径

最朴素的做法是枚举所有子串，判断每个是否是回文，取最长的。但子串个数是 `O(n²)`，每个判断要 `O(n)`，总复杂度 `O(n³)`。

观察回文的性质：如果一个字符串是回文，那么去掉首尾两个字符后，剩下的也是回文。反过来，如果已知中间一段是回文，只要首尾两个字符相同，就能向外扩展一圈。这就是"中心扩展法"的思路。

另一种方式是动态规划。定义 `dp[i][j]` 表示 `s[i..j]` 是否是回文。转移方程是 `dp[i][j] = (s[i] == s[j]) and dp[i+1][j-1]`。按子串长度从小到大填表即可。

## 💻 Java 题解

### 解法 1：中心扩展法

对每个中心（奇数和偶数两种），向两边扩展找最长回文。

- ⏱ 时间复杂度：`O(n²)`
- 💾 空间复杂度：`O(1)`

```java
class Solution {
    public String longestPalindrome(String s) {
        int start = 0, maxLen = 1;
        for (int i = 0; i < s.length(); i++) {
            int len1 = expand(s, i, i);     // 奇数
            int len2 = expand(s, i, i + 1); // 偶数
            int len = Math.max(len1, len2);
            if (len > maxLen) {
                maxLen = len;
                start = i - (len - 1) / 2;
            }
        }
        return s.substring(start, start + maxLen);
    }
    private int expand(String s, int l, int r) {
        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { l--; r++; }
        return r - l - 1;
    }
}
```

**关键点：**
- 两种中心：i和i,i+1
- expand返回长度
- start = i-(len-1)/2

**执行步骤：**
1. 遍历每个中心
1. 奇偶两种扩展
1. 更新最大值和起点


## ⚠️ 易错点

- 中心扩展必须同时考虑奇数和偶数长度两种情况。奇数以单个字符为中心，偶数以两个相邻字符的间隙为中心。
- 扩展结束后，`left` 和 `right` 都多走了一步，所以实际的回文区间是 `[left+1, right-1]`。
- 单字符字符串本身就是回文，需要单独处理或保证循环正确覆盖。

- start = i-(len-1)/2 不是 i-len/2

- expand返回r-l-1 不是r-l+1

## 📊 复杂度分析

- 时间复杂度：`O(n²)`。每个字符作为中心最多扩展 `O(n)` 次。
- 空间复杂度：`O(1)`。只用了常数个变量。

## 🏢 实际业务应用

- DNA 序列分析：寻找基因序列中的回文片段，回文结构在 DNA 双链的稳定性和酶切位点识别中很重要。
- 文本纠错与模式匹配：回文检测用于识别文本中的对称结构，如自然语言处理中的对称短语检测。
- 数据压缩：某些压缩算法利用回文子串的重复性来做字符串的去重和编码优化。
- 网络安全：恶意代码检测中，某些混淆技术会利用回文结构来隐藏关键字符串，检测回文模式可以辅助识别。

- DNA 序列分析：寻找基因序列中的回文片段，回文结构在 DNA 双链的稳定性和酶切位点识别中很重要。
- 文本纠错与模式匹配：回文检测用于识别文本中的对称结构，如自然语言处理中的对称短语检测。
- 数据压缩：某些压缩算法利用回文子串的重复性来做字符串的去重和编码优化。
- 网络安全：恶意代码检测中，某些混淆技术会利用回文结构来隐藏关键字符串，检测回文模式可以辅助识别。

## 📖 形象类比

可以类比《红楼梦》里园林的镜像对称设计。

大观园的设计讲究中轴对称。贾政领人游览时，站在中轴线上的某一点，向左右两边看，如果左边的亭台和右边的楼阁完全对称，说明这一段是"回文结构"。他每走到一个新位置，就从那里向左右两侧同时展开比对，看能对称延伸多远。如果某处左右不对称了，就停下来，记录这段对称区域有多长。

对应关系是：

- "园林的建筑序列"对应字符串 `s`
- "中轴线上的站位"对应回文中心 `i`
- "向左右两侧同时比对"对应中心扩展函数 `expand`
- "对称区域的最长长度"对应 `maxLen`

记忆的关键是：站在中心往两边看，对称就继续扩，不对称就停。

可以类比《红楼梦》里园林的镜像对称设计。

大观园的设计讲究中轴对称。贾政领人游览时，站在中轴线上的某一点，向左右两边看，如果左边的亭台和右边的楼阁完全对称，说明这一段是"回文结构"。他每走到一个新位置，就从那里向左右两侧同时展开比对，看能对称延伸多远。如果某处左右不对称了，就停下来，记录这段对称区域有多长。

对应关系是：

- "园林的建筑序列"对应字符串 `s`
- "中轴线上的站位"对应回文中心 `i`
- "向左右两侧同时比对"对应中心扩展函数 `expand`
- "对称区域的最长长度"对应 `maxLen`

记忆的关键是：站在中心往两边看，对称就继续扩，不对称就停。

小蚂蚁在一片树叶上发现了一排露珠，每颗露珠上倒映着不同的花纹。它想知道哪一段连续的露珠从两头往中间看花纹完全一样。小蚂蚁从每颗露珠出发，同时向左右两颗看：如果花纹一样，就再往外看一颗，直到花纹不同为止。一路试完所有露珠，记下最长的对称段。

小蚂蚁在一片树叶上发现了一排露珠，每颗露珠上倒映着不同的花纹。它想知道哪一段连续的露珠从两头往中间看花纹完全一样。小蚂蚁从每颗露珠出发，同时向左右两颗看：如果花纹一样，就再往外看一颗，直到花纹不同为止。一路试完所有露珠，记下最长的对称段。

## 🎵 记忆口诀

中心向两侧扩，奇偶都要试。

中心向两侧扩，奇偶都要试。

## 🔗 延伸练习

- `647. 回文子串`：不只是找最长的，而是统计所有回文子串的个数，同样的中心扩展思路。
- `516. 最长回文子序列`：从"子串"（连续）变成"子序列"（可不连续），需要用动态规划。

- `647. 回文子串`：不只是找最长的，而是统计所有回文子串的个数，同样的中心扩展思路。
- `516. 最长回文子序列`：从"子串"（连续）变成"子序列"（可不连续），需要用动态规划。
