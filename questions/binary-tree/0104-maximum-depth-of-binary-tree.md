---
id: "0104"
slug: "maximum-depth-of-binary-tree"
lcId: 104
title: "二叉树的最大深度"
difficulty: "Easy"
category: "binary-tree"
tags:
  - "树"
  - "深度优先搜索"
  - "广度优先搜索"
  - "二叉树"
url: "https://leetcode.cn/problems/maximum-depth-of-binary-tree/"
planOrder: 37
hasViz: true
hasDiagram: false
feynman:
  essence: "给你一棵二叉树的根节点 `root`，返回它的最大深度。  最大深度是从根节点到最远叶子节点的最长路径上的节点数。"
  analogy: ""
  key_points: []
first_principle:
  problem: ""
  axioms: []
  rebuild: ""
---

# 104. 二叉树的最大深度

> 难度：**Easy** | 分类：二叉树 | 标签：树, 深度优先搜索, 广度优先搜索, 二叉树

> 🔗 力扣链接：[https://leetcode.cn/problems/maximum-depth-of-binary-tree/](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)

## 📝 题目描述

给你一棵二叉树的根节点 `root`，返回它的最大深度。

最大深度是从根节点到最远叶子节点的最长路径上的节点数。

- 题号：104
- 题目名：二叉树的最大深度
- 难度：简单
- 题型：二叉树
- 为什么选这题：它是二叉树递归最基础的练手题，"当前节点深度 = 1 + max(左子树深度, 右子树深度)"这个公式是后续所有树高相关问题的根基。

## 🧠 思考路径

一棵树的最大深度 = 1（根节点本身）+ max(左子树的最大深度, 右子树的最大深度)。

如果节点为空，深度为 0。这就是递归的全部逻辑。

也可以用 BFS（层序遍历），数一共有多少层就是最大深度。但递归写法更简洁，也更符合直觉。

## 💻 Java 题解

### 解法 1：DFS 递归

树的深度 = max(左子树深度, 右子树深度) + 1。空节点深度为0。

- ⏱ 时间复杂度：`O(n)`
- 💾 空间复杂度：`O(h)`

```java
class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) return 0;
        return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));
    }
}
```

**关键点：**
- max(left, right) + 1
- 空节点返回0

**执行步骤：**
1. 空节点返回0
1. 递归求左右子树深度
1. 取最大值+1


### 解法 2：BFS 层序遍历

逐层遍历，层数即为深度。

- ⏱ 时间复杂度：`O(n)`
- 💾 空间复杂度：`O(n)`

```java
class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) return 0;
        Queue<TreeNode> q = new LinkedList<>();
        q.offer(root);
        int depth = 0;
        while (!q.isEmpty()) {
            int size = q.size();
            depth++;
            for (int i = 0; i < size; i++) {
                TreeNode node = q.poll();
                if (node.left != null) q.offer(node.left);
                if (node.right != null) q.offer(node.right);
            }
        }
        return depth;
    }
}
```

**关键点：**
- BFS 逐层计数
- 每层 size 固定后再处理

**执行步骤：**
1. 入队根节点
1. 逐层弹出并计数
1. 子节点入队


## ⚠️ 易错点

- 空树的深度是 0，不是 -1 或 1。根节点单独存在时深度是 1（`0 + 1 = 1`）。
- BFS 写法中 `depth += 1` 放在外层循环（每层开始时），不是内层循环。
- 递归法虽然简洁，但极端情况（链状树，`n = 10⁴`）可能导致栈溢出。Python 默认递归深度约 1000，可能需要 `sys.setrecursionlimit`。

- 递归终止条件 root==null 返回0

## 📊 复杂度分析

- 时间复杂度：`O(n)`。每个节点访问一次。
- 空间复杂度：`O(h)`（递归）或 `O(n)`（BFS）。递归栈深度等于树高。

## 🏢 实际业务应用

- 文件系统目录深度计算：操作系统需要知道目录嵌套的最大深度，用于设置路径长度限制或优化查找策略。
- DOM 树渲染深度分析：浏览器渲染引擎计算 DOM 树的最大深度，用于性能优化和布局计算。
- 组织架构层级统计：HR 系统需要知道公司组织架构的最大汇报层级数。
- 嵌套 JSON 深度检测：API 框架限制请求体的嵌套深度以防止恶意构造的超深 JSON。

- 文件系统目录深度计算：操作系统需要知道目录嵌套的最大深度，用于设置路径长度限制或优化查找策略。
- DOM 树渲染深度分析：浏览器渲染引擎计算 DOM 树的最大深度，用于性能优化和布局计算。
- 组织架构层级统计：HR 系统需要知道公司组织架构的最大汇报层级数。
- 嵌套 JSON 深度检测：API 框架限制请求体的嵌套深度以防止恶意构造的超深 JSON。

## 📖 形象类比

可以把它类比成《西游记》里唐僧师徒走九九八十一难中寻找最长路途的场景。

取经路上有无数岔路口（二叉树），每个岔路口可以往左走或往右走。猪八戒的任务是找出从长安到灵山的最多磨难路线——即经过最多的岔路口。

八戒的策略很简单：在每个岔路口，先让小妖去探左边有多远，再探右边有多远，取更远的那条路加上当前这一步，就是从这里出发能走的最远距离。

对应关系是：

- "二叉树"对应取经路上的岔路口网络
- "最大深度"对应最多磨难的路线
- "`max(左, 右)`"对应选择左右中更远的那条
- "`1 + max`"对应加上当前这一步

这段记忆的关键是：深度 = 当前这一步 + 左右子树中更深的那边。

可以把它类比成《西游记》里唐僧师徒走九九八十一难中寻找最长路途的场景。

取经路上有无数岔路口（二叉树），每个岔路口可以往左走或往右走。猪八戒的任务是找出从长安到灵山的最多磨难路线——即经过最多的岔路口。

八戒的策略很简单：在每个岔路口，先让小妖去探左边有多远，再探右边有多远，取更远的那条路加上当前这一步，就是从这里出发能走的最远距离。

对应关系是：

- "二叉树"对应取经路上的岔路口网络
- "最大深度"对应最多磨难的路线
- "`max(左, 右)`"对应选择左右中更远的那条
- "`1 + max`"对应加上当前这一步

这段记忆的关键是：深度 = 当前这一步 + 左右子树中更深的那边。

小鼹鼠要在地洞里挖最深的洞。它从地面开始，每个洞口可以往左挖或往右挖。小鼹鼠让弟弟先去左边的洞看看有多深，让妹妹去右边的洞看看有多深。弟弟回来说"左边最深 3 米"，妹妹回来说"右边最深 5 米"。小鼹鼠选了右边的路继续挖，加上当前这一米，总共能挖 6 米深。

小鼹鼠要在地洞里挖最深的洞。它从地面开始，每个洞口可以往左挖或往右挖。小鼹鼠让弟弟先去左边的洞看看有多深，让妹妹去右边的洞看看有多深。弟弟回来说"左边最深 3 米"，妹妹回来说"右边最深 5 米"。小鼹鼠选了右边的路继续挖，加上当前这一米，总共能挖 6 米深。

## 🎵 记忆口诀

空零非空一加深，左右取大往上增。

空零非空一加深，左右取大往上增。

## 🔗 延伸练习

- `111. 二叉树的最小深度`：把 `max` 换成 `min`，但要小心单支树的情况。
- `110. 平衡二叉树`：判断左右子树深度差是否不超过 1，复用深度计算逻辑。

- `111. 二叉树的最小深度`：把 `max` 换成 `min`，但要小心单支树的情况。
- `110. 平衡二叉树`：判断左右子树深度差是否不超过 1，复用深度计算逻辑。
