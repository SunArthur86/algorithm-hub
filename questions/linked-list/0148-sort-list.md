---
id: "0148"
slug: "sort-list"
lcId: 148
title: "排序链表"
difficulty: "Medium"
category: "linked-list"
tags:
  - "链表"
  - "双指针"
  - "分治"
  - "排序"
  - "归并排序"
url: "https://leetcode.cn/problems/sort-list/"
planOrder: 33
hasViz: true
hasDiagram: false
feynman:
  essence: "给定链表的头节点 `head`，请将其按升序排列并返回排序后的链表。  要求时间复杂度 `O(n log n)`，空间复杂度 `O(1)`（即不能用递归，因为递归栈是 `O(log n)`）。重点不在"
  analogy: ""
  key_points: []
first_principle:
  problem: ""
  axioms: []
  rebuild: ""
---

# 148. 排序链表

> 难度：**Medium** | 分类：链表 | 标签：链表, 双指针, 分治, 排序, 归并排序

> 🔗 力扣链接：[https://leetcode.cn/problems/sort-list/](https://leetcode.cn/problems/sort-list/)

## 📝 题目描述

给定链表的头节点 `head`，请将其按升序排列并返回排序后的链表。

要求时间复杂度 `O(n log n)`，空间复杂度 `O(1)`（即不能用递归，因为递归栈是 `O(log n)`）。重点不在"排序"，而在"怎么在链表上实现归并排序"。

- 题号：148
- 题目名：排序链表
- 难度：中等
- 题型：链表
- 为什么选这题：它要求 `O(n log n)` 时间 + `O(1)` 空间排序链表，是"归并排序在链表上的应用"的代表作。

## 🧠 思考路径

插入排序是 `O(n²)`，不符合要求。快速排序在链表上实现较复杂（不方便随机取 pivot）。

归并排序天然适合链表：
1. **分割**：用快慢指针找到链表中点，分成两半。
2. **递归排序**：对两半分别排序。
3. **合并**：把两个有序链表合并成一个。

但题目要求 `O(1)` 空间，递归的调用栈是 `O(log n)`。可以用**自底向上的迭代归并**：先 1 个 1 个合并，再 2 个 2 个合并，再 4 个 4 个合并……直到合并完整个链表。

这样不需要递归，空间是 `O(1)`。

所以这题的关键词不是"排序"，而是"链表上的归并排序：找中点 + 合并两个有序链表"。

## 💻 Java 题解

### 解法 1：归并排序（递归）

快慢指针找中点切分，递归排序左右两半，然后合并两个有序链表。

- ⏱ 时间复杂度：`O(n log n)`
- 💾 空间复杂度：`O(log n)`

```java
class Solution {
    public ListNode sortList(ListNode head) {
        if (head == null || head.next == null) return head;
        // 找中点
        ListNode slow = head, fast = head.next;
        while (fast != null && fast.next != null) {
            slow = slow.next; fast = fast.next.next;
        }
        ListNode mid = slow.next;
        slow.next = null;
        // 递归排序
        ListNode left = sortList(head);
        ListNode right = sortList(mid);
        // 合并
        return merge(left, right);
    }
    private ListNode merge(ListNode a, ListNode b) {
        ListNode dummy = new ListNode(0), tail = dummy;
        while (a != null && b != null) {
            if (a.val <= b.val) { tail.next = a; a = a.next; }
            else { tail.next = b; b = b.next; }
            tail = tail.next;
        }
        tail.next = a != null ? a : b;
        return dummy.next;
    }
}
```

**关键点：**
- 快慢指针找中点
- 递归分治
- 合并两个有序链表

**执行步骤：**
1. 快慢指针找中点
1. 断开成两段
1. 递归排序左右
1. 合并


## ⚠️ 易错点

- 找中点时，`fast` 从 `head.next` 开始（不是 `head`），确保奇数长度时 slow 停在左半的末尾。
- 断开链表时 `slow.next = None` 不能忘。
- 合并时用哨兵节点 `dummy` 简化操作。
- 自底向上归并中，`split` 函数需要在断开处截断（`head.next = null`）。

- fast 初始是 head.next 不是 head（避免栈溢出）

- slow.next=null 断开链表

## 📊 复杂度分析

- 时间复杂度：`O(n log n)`。归并排序的标准复杂度。
- 空间复杂度：递归版本 `O(log n)`（递归栈），迭代版本 `O(1)`。

## 🏢 实际业务应用

- 大规模日志排序：日志以链表形式存储（每条日志指向下一条），需要按时间戳排序时，归并排序是链表上最优的选择。
- 数据库的外排序：当数据量大到内存放不下时，数据以链表形式分块存储，块间用归并排序合并。
- 大文件合并：多个有序的小文件合并成一个大文件，核心就是链表归并。

- 大规模日志排序：日志以链表形式存储（每条日志指向下一条），需要按时间戳排序时，归并排序是链表上最优的选择。
- 数据库的外排序：当数据量大到内存放不下时，数据以链表形式分块存储，块间用归并排序合并。
- 大文件合并：多个有序的小文件合并成一个大文件，核心就是链表归并。

## 📖 形象类比

可以把它类比成《三国演义》里诸葛亮整编投降的散兵。

散兵按战斗力排成一个无序列表。诸葛亮的办法是：先把队伍分成两半，每半再分两半，直到每队只有一个人（天然有序）。然后两两合并：每次取两边较弱的排前面，较强的排后面。合并完的小队再和其他小队合并，最终整支大军从弱到强排列整齐。

对应关系是：

- "散兵列表"对应无序链表
- "分成两半"对应快慢指针找中点
- "每队只有一个人"对应递归基准情况
- "两两合并，弱的在前"对应合并两个有序链表

这段记忆的关键是：分到不能再分，再两两合回来。

可以把它类比成《三国演义》里诸葛亮整编投降的散兵。

散兵按战斗力排成一个无序列表。诸葛亮的办法是：先把队伍分成两半，每半再分两半，直到每队只有一个人（天然有序）。然后两两合并：每次取两边较弱的排前面，较强的排后面。合并完的小队再和其他小队合并，最终整支大军从弱到强排列整齐。

对应关系是：

- "散兵列表"对应无序链表
- "分成两半"对应快慢指针找中点
- "每队只有一个人"对应递归基准情况
- "两两合并，弱的在前"对应合并两个有序链表

这段记忆的关键是：分到不能再分，再两两合回来。

小蚂蚁要把路上散落的面包屑按大小排好。它先把面包屑堆分成两堆，每堆再分两堆，分到每堆只有一颗。然后两颗两颗比大小合并，小的在前大的在后；合并好的小堆再和其他小堆比，继续合并。最后所有面包屑从最小到最大排成了一条整齐的队伍。

小蚂蚁要把路上散落的面包屑按大小排好。它先把面包屑堆分成两堆，每堆再分两堆，分到每堆只有一颗。然后两颗两颗比大小合并，小的在前大的在后；合并好的小堆再和其他小堆比，继续合并。最后所有面包屑从最小到最大排成了一条整齐的队伍。

## 🎵 记忆口诀

快慢分中点，递归排两半；有序链表双指合并，分分合合即排序。

快慢分中点，递归排两半；有序链表双指合并，分分合合即排序。

## 🔗 延伸练习

- `21. 合并两个有序链表`：本题合并步骤的基础，独立的链表合并练习。
- `147. 对链表进行插入排序`：对比 `O(n²)` 和 `O(n log n)` 排序在链表上的差异。

- `21. 合并两个有序链表`：本题合并步骤的基础，独立的链表合并练习。
- `147. 对链表进行插入排序`：对比 `O(n²)` 和 `O(n log n)` 排序在链表上的差异。
